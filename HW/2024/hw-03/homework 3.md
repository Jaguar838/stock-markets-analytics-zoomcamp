## Домашнє завдання до модуля 3

У цьому домашньому завданні ми будемо працювати з категоріальними змінними, першими моделями машинного навчання (деревами рішень) та налаштуванням гіперпараметрів.

Будь ласка, використовуйте [Colab Module 3](https://github.com/DataTalksClub/stock-markets-analytics-zoomcamp/blob/main/03-modeling/Module_3_Colab_Time_Series_Modeling.ipynb) для всіх завдань, щоб переконатися, що у вас такий же датафрейм, який використовується в частині Моделювання, як це було розглянуто під час лекції. Рекомендуємо копіювати та розширювати його (навколо коментарів "TODO").

**ПІДКАЗКА**: Якщо ви хочете уникнути скорочення даних у інтерфейсі GitHub, спробуйте один з наступних варіантів:
* Відкрийте блокнот у [Colab, використовуючи посилання на блокнот у GitHub](https://colab.research.google.com/github/DataTalksClub/stock-markets-analytics-zoomcamp/blob/main/03-modeling/Module_3_Colab_Time_Series_Modeling.ipynb).
* Клонуйте репозиторій у локальну папку та відкрийте блокнот у Jupyter Notebook.
---
### Питання 1 (1 бал): Даммі-змінні на місяць та тиждень місяця

**Знайдіть АБСОЛЮТНЕ ЗНАЧЕННЯ КОРЕЛЯЦІЇ найбільш корельованої даммі-змінної <month-week_of_month> з бінарною вихідною змінною `is_positive_growth_5d_future`?**

У аналізі кореляції та моделюванні ви побачили, що вересень і жовтень можуть бути важливими сезонними місяцями. У цьому завданні ми підемо далі і спробуємо згенерувати даммі-змінні для місяця і тижня місяця (починаючи з 1). Наприклад, перший тиждень жовтня повинен бути закодований подібно до цього: 'October_w1'.
Як тільки ви згенеруєте новий набір змінних, знайдіть найбільш корельовану (за абсолютним значенням) з `is_positive_growth_5d_future` і округліть її до 3 знаків після коми.

Пропонований шлях до рішення:
- [[Джерело](https://stackoverflow.com/questions/25249033/week-of-a-month-pandas)] Використовуйте цю формулу, щоб отримати тиждень місяця для змінної дати d: `(d.day-1)//7+1`
- Визначте нову рядкову змінну для всіх комбінацій місяць-тиждень_місяця. Додайте її до набору КАТЕГОРІАЛЬНИХ змінних. У вас повинно бути 5 змінних, які вважаються КАТЕГОРІАЛЬНИМИ зараз: 'Month', 'Weekday', 'Ticker', 'ticker_type', 'month_wom'. У кінцевому результаті ви повинні отримати 115 даммі-змінних, включаючи 60 (=12*5) даммі-змінних для тижнів місяця.
- Використовуйте [pandas.get_dummies()](https://pandas.pydata.org/pandas-docs/stable/reference/api/pandas.get_dummies.html) для генерації даммі-змінних.
- Використовуйте функцію [pandas.DataFrame.corr()](https://pandas.pydata.org/docs/reference/api/pandas.DataFrame.corr.html) (також використану в [Code Snippet 1]) для отримання кореляцій з `is_positive_growth_5d_future`, відфільтруйте тільки змінні, що представляють новий набір даммі-змінних, та відсортуйте їх за абсолютними значеннями (ви можете визначити новий стовпець "abs_corr" у датафреймі з кореляціями) і знайдіть найбільше значення (серед нового набору даммі-змінних).

**ПРИМІТКА**: нові даммі-змінні будуть використовуватися як ознаки в наступних завданнях, будь ласка, залиште їх у наборі даних.

---
### Питання 2 (2 бали): Визначення нових "ручних" правил на макроекономічних та технічних індикаторах

**Яка точність (precision score) найкращого з НОВИХ прогнозів (pred3 або pred4), округлена до 3 знаків після коми?**

Використовуючи знання з візуалізованого дерева (clf10) (Code Snippet 5: 1.4.4 Візуалізація):

* Ви повинні визначити два нових 'ручних' правила (ведуть до 'позитивних' піддерев):
  - `pred3_manual_gdp_fastd`: (gdppot_us_yoy <= 0.027) & (fastd >= 0.251)
  - `pred4_manual_gdp_wti_oil`: (gdppot_us_yoy >= 0.027) & (growth_wti_oil_30d <= 1.005)

* Розширте Code Snippet 3 (Прогнози "ручного" правила): Обчисліть та додайте нові правила (pred3 і pred4) до датафрейму. Ви повинні помітити, що один із прогнозів не має жодних позитивних прогнозів на ТЕСТОВОМУ наборі даних (хоча їх багато на TRAIN+VALIDATION).

* Налагодження: перевірте у `new_df` та оригінальному наборі даних/процесі генерації даних, що ми не зробили жодних помилок під час етапу перетворення даних.

* Поясніть, чому це може статися, навіть якщо в ознаках даних немає помилок.

* У результаті запишіть точність (precision score) для залишеного предиктора (округлену до трьох знаків після коми). Наприклад, якщо у вас 0.57897, ваша відповідь повинна бути 0.579.

---
### Питання 3 (1 бал): Унікальні правильні прогнози з 10-рівневого дерева рішень (pred5_clf_10)

**Яка загальна кількість записів у ТЕСТОВОМУ наборі даних, коли новий прогноз pred5_clf_10 краще за всі 'ручні' правила (pred0..pred4)?**

ПРИМІТКА: будь ласка, включіть `random_state=42` до функції ініціалізації Decision Tree Classifier (рядок `clf = DecisionTreeClassifier(max_depth=max_depth, random_state=42)`) щоб забезпечити однакові результати для всіх.

Пропоноване рішення:
* Крок 1: Перепишіть частину '1.4.3 Inference for a decision tree' для Decision Tree Classifier з max_depth=10 (clf_10), щоб ви навчали модель на TRAIN+VALIDATION наборах (без змін від лекції), але прогнозували на всьому наборі X_all (щоб мати можливість визначити новий стовпець 'pred5_clf_10' у датафреймі new_df). Ось [посилання](https://stackoverflow.com/questions/40729162/merging-results-from-model-predict-with-original-pandas-dataframe) з поясненням. Це вирішить проблему у 1.4.5, коли прогнози робилися тільки для Тестового набору і їх не можна було легко приєднати до повного набору даних.

* Крок 2: Як тільки ви це зробите, визначте новий стовпець 'only_pred5_is_correct', подібно до 'ручних' правил прогнозування з кількома умовами: is_positive_growth_5d_future І is_correct_pred5 повинні бути рівні 1, тоді як всі інші прогнози is_correct_pred0..is_correct_pred4 повинні бути рівні 0.

* Крок 3: Перетворіть стовпець 'only_pred5_is_correct' з bool у int і знайдіть, скільки разів він дорівнює 1 у ТЕСТОВОМУ наборі. Запишіть це як відповідь.

ДОДАТКОВО: визначте функцію, яка може бути застосована до всього рядка прогнозів ([декілька прикладів pandas-apply-row-функцій](https://sparkbyexamples.com/pandas/pandas-apply-function-to-every-row/)) і може визначити, чи є якийсь прогноз 'pred

X' (де X є одним із прогнозів) унікально правильним. Вона повинна працювати навіть якщо доступно 100 прогнозів, щоб ви не визначали вручну умову для 'predX'.

---
### Питання 4 (2 бали): Налаштування гіперпараметрів для дерева рішень

**Яка оптимальна глибина дерева (від 1 до 20) для DecisionTreeClassifier?**

ПРИМІТКА: будь ласка, включіть `random_state=42` до функції ініціалізації Decision Tree Classifier (рядок `clf = DecisionTreeClassifier(max_depth=max_depth, random_state=42)`) щоб забезпечити однакові результати для всіх.

Виконайте ці кроки, щоб знайти оптимальний `max_depth`:
* Ітеруйте через значення `max_depth` від 1 до 20.
* Навчіть Decision Tree Classifier з поточним параметром `max_depth`.
* Опційно, візуалізуйте, як змінюється 'голова' кожного навченного дерева з більш просунутими (=глибшими) деревами. Ви можете використовувати функцію [`sklearn.tree.plot_tree()`](https://scikit-learn.org/stable/modules/generated/sklearn.tree.plot_tree.html), або компактний спосіб з функціональністю `export_text()` ([Приклад з Stack Overflow](https://stackoverflow.com/questions/20156951/how-do-i-find-which-attributes-my-tree-splits-on-when-using-scikit-learn)):
  ```
  from sklearn.tree import export_text
  tree_rules = export_text(model, feature_names=list(X_train), max_depth=3)
  print(tree_rules)
  ```
* Розрахуйте точність (precision score) (ви можете використовувати функцію [sklearn.metrics.precision_score()](https://scikit-learn.org/stable/modules/generated/sklearn.metrics.precision_score.html)) на ТЕСТОВОМУ наборі даних для кожного з навчальних дерев. Ви також можете порівняти це з точністю на ВАЛІДАЦІЙНОМУ наборі, який включений до навчальної фази (щоб мати більше даних для навчання). Ви повинні побачити, що точність на ВАЛІДАЦІЙНОМУ наборі починає зростати з ускладненням дерева (перенавчання), чого не видно на точності на ТЕСТОВОМУ наборі.
* Визначте оптимальний `max_depth`, де точність є найвищою на ТЕСТОВОМУ наборі. Запишіть це значення як **best_max_depth** і надішліть як відповідь.
* Зробіть прогнози на всіх записах (TRAIN+VALIDATION+TEST) та додайте новий прогноз `pred6_clf_best` до датафрейму `new_df`.

Додатково, порівняйте точність налаштованого дерева рішень з попередніми прогнозами. Ви повинні побачити покращення (>0.58, або більше 58% точності), що вказує на те, що налаштоване дерево перевершує попередні "ручні" правила та прогнози дерева рішень.

ДОДАТКОВО: Дізнайтеся більше про різні аспекти [дерев рішень scikit-learn](https://scikit-learn.org/stable/modules/tree.html). Намалюйте графік точності/прецизійності в залежності від max_depth і зверніть увагу, чи є точка насичення точності/прецизійності з підвищенням max_depth. Теоретично, повинна бути компроміс між кращим підходом (=більш складними деревами) та узагальненням.

---
### [ДОСЛІДЖУВАНЕ] Питання 5: Які дані відсутні?

Тепер, коли у вас є деякі інсайти з аналізу кореляції та дерев рішень щодо найбільш впливових змінних, запропонуйте нові індикатори, які ви хотіли б включити до набору даних і поясніть, чому.

Ви також можете запропонувати щось абсолютно інше, ґрунтуючись на вашій інтуїції, але це повинно бути релевантним до спільного набору даних найбільших індійських, європейських та американських акцій. Якщо ви обираєте цей підхід, будь ласка, вкажіть джерело даних.

---
## Надсилання рішень

Форма для надсилання: https://courses.datatalks.club/sma-zoomcamp-2024/homework/hw03

---
## Таблиця лідерів

Посилання на таблицю лідерів: https://courses.datatalks.club/sma-zoomcamp-2024/leaderboard